# Bench task 3: Debug a failing test (high variance)

Given a repo with one failing test where the root cause is in a file the test does not directly import: find the cause, fix it, get tests green.

Run TWICE — single-rep results on debug tasks are unreliable (adam-s/testing-claude-agent showed 4,121 vs 15,409 tokens between runs of the same task). Average.

Pass criteria for the skill: lower average tokens than baseline run (no skill loaded), with tests green in both runs.
