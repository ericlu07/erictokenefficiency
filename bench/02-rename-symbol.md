# Bench task 2: Rename symbol across files

Rename the function `getCwd` to `getCurrentWorkingDirectory` everywhere it appears in the target repo. Update all callers. Run tests. Report tokens.

Run twice on a clean clone. Average the result.
